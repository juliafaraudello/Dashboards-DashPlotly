<!doctype html>
<html>
<head>
    <meta charset="utf-8">
    <style type="text/css">
        :root {
            --font-family-sans-serif: -apple-system, BlinkMacSystemFont, "Segoe UI", Gotham, Roboto, "Helvetica Neue", Arial, sans-serif;
        }

        body, td, th {
            /*font-family: Gotham, "Helvetica Neue", Helvetica, Arial, sans-serif;*/
            font-family: var(--font-family-sans-serif);
            font-size: 12px;
        }

        ul {
            padding-left: 30px;
        }

        li {
            padding: 0.25rem 0;
        }

            li:last-child {
                padding-bottom: 0.5rem;
            }

        h1 {
            color: #FFF;
            font-size: 15px;
        }

        body {
            background-color: #E2E2E2;
            width: 100%;
        }

        #table {
            border: 0;
            padding: 1rem;
        }

        #table tbody {
            border-radius: 0.5rem;
            box-shadow: 1px 1px 4px 0 #adadad;
            -moz-box-shadow: 1px 1px 4px 0 #adadad;
            -webkit-box-shadow: 1px 1px 4px 0 #adadad;
            display: inline-block;
            overflow: hidden;
        }

            
        #table tr td {
            background-color: #E2E2E2;
        }

        .header {
            height: 3rem;
        }

            .header td {
                background-color: #2B723A !important;
                padding: 0 1.3rem;
            }

        .message td {
            background-color: #FFFFFF !important;
            padding: 0.6rem 1.3rem;
        }

        #title {
            display: inline-block;
            line-height: 3rem;
            margin: 0;
        }

        #icon {
            float: right;
            height: 39px;
            padding: 0.25rem 0;
            width: 39px;
        }

        #salute {
            font-size: 13px;
        }
    </style>
</head>
<body>
    <table id="table" align="center" cellpadding="0" cellspacing="0">
        <tr class="header">
            <td>
                <h1 id="title"><strong>O acesso não pôde ser autorizado</strong></h1>
            </td>
        </tr>
        <tr class="message">
            <td>
                <p dir="ltr">
                    Não tem permissões suficientes para acessar o painel. Entre em contato com um administrador.
                </p>
                <ul>
                    <a href="{{URL}}">Reautenticar</a>
                </ul>
                <p dir="ltr">
                    Cordialmente,<br>
                    <strong>A equipe campo 360.</strong><br>
                </p>
            </td>
        </tr>
    </table>
</body>
</html>